# AryaaOS — GO. The single canonical action list.

This is the only doc you need open while executing. Every other file is reference material.

Read top to bottom. Don't skip steps. ~3 hours total. You'll be away from the screen for ~30 min while Claude Code scaffolds; that's normal.

---

## STEP 0 — Set the table (5 min, you)

- [ ] Open Bitwarden / 1Password / browser password manager
- [ ] Create a secure note titled **"AryaaOS secrets"** — Claude for Chrome will paste credentials in here as it goes
- [ ] Open `awish@layer1.tech` Outlook in another tab (verification emails)
- [ ] Phone unlocked, near you (SMS codes)
- [ ] Credit card in hand
- [ ] Open Claude for Chrome (extension active in browser)
- [ ] Open `04-claude-for-chrome-prompts.md` in another tab — you'll copy from it

When all six boxes are checked, start STEP 1.

---

## HOUR 1 — Account creation batch (~50 min)

Each step: copy the prompt block from `04-claude-for-chrome-prompts.md`, paste into Claude for Chrome, sit nearby for SMS / OAuth click-through. CfC tells you when each is done.

### STEP 1 — GitHub repo (3 min)
**Paste prompt §A1.**
Result: empty private repo `awish4u-jpg/aryaaos`.

### STEP 1.5 — GitHub hardening (5 min, you do this directly)

Don't paste a CfC prompt here — these are quick clicks in the GitHub UI.

**Account level** (`github.com/settings/security`):
- [ ] **2FA enabled** (authenticator app, not SMS)
- [ ] **Audit Authorized OAuth Apps** — revoke anything older than 6 months you don't recognize
- [ ] **Audit Installed GitHub Apps** — review each, scope to specific repos only, revoke anything unfamiliar (including **WhatProblem AI** if you decided to remove it earlier)

**Repo level** (`github.com/awish4u-jpg/aryaaos/settings/security_analysis`):
- [ ] Enable **Dependabot alerts** (already on by default for private repos — just confirm)
- [ ] Enable **Dependabot security updates** (auto-PRs for security fixes)
- [ ] Enable **Dependabot version updates** — create `.github/dependabot.yml` later in build
- [ ] Enable **Secret scanning** (free for private repos as of GitHub's update)
- [ ] Enable **Push protection** (blocks commits that contain secrets before they're pushed)

**Apps that will auto-install during Day 1 setup** (no action — just so you know):
- Vercel for GitHub (in STEP 4) — auto-deploys
- Convex (in STEP 2) — preview deploys
- Sentry (in STEP 10) — error source maps

All three will be scoped to `awish4u-jpg/aryaaos` only. CfC enforces that during their OAuth steps.

**Apps to NOT install**:
- GitHub Copilot (Claude Code covers it)
- CodeRabbit / Codiumate / Greptile (duplicate of Claude review)
- Any "AI debugger" app you don't recognize

### STEP 2 — Convex (5 min)
**Paste prompt §A2.**
- OAuth-approve when GitHub prompts.
Result: project URL + deploy key in staging note.

### STEP 3 — Anthropic Console (10 min) ⚠️ revised caps
**Paste prompt §A3 BUT change two values:**
- Monthly Spend Limit: **$100** (not $200)
- Daily Spend Notification: **$5** (not $10)

The daily notification is a free email alert, not a charge. The monthly limit is the hard fuse.

Result: API key in staging note.

### STEP 4 — Vercel (5 min)
**Paste prompt §A4.**
- OAuth-approve when GitHub prompts.
- Add domain `aryaaos.casaaria.in` — Vercel will say "Invalid Configuration." Ignore for now.
Result: CNAME target value (e.g., `cname.vercel-dns.com`) in staging note.

### STEP 5 — ❌ SKIP Voyage
We're using OpenAI text-embedding-3-small instead. 3x cheaper, same quality. No Voyage account needed.

### STEP 6 — Google AI Studio / Gemini (8 min) ⚠️ revised cap
**Paste prompt §A6 BUT change one value:**
- Budget alert: **$10/mo** (not $50)

Result: Gemini API key. Also confirms Nano Banana (Gemini 2.5 Image) is enabled.

### STEP 7 — OpenAI (5 min) ⚠️ revised cap
**Paste prompt §A7 BUT change one value:**
- Hard limit: **$30/mo** (not $20)

This account now covers BOTH Whisper API AND text-embedding-3-small.

Result: OpenAI API key in staging note.

### STEP 8 — Wispr Flow Windows (5 min) ⚠️ verify availability
**Paste prompt §A8.**

**FIRST thing CfC does: confirm Windows is supported on `wisprflow.ai/download`.**

- If YES → CfC downloads installer (you install it later in STEP 20).
- If NO → STOP. Reply in chat: "Wispr is Mac-only." We switch to Aqua Voice (`aqua.ai`).

Don't proceed to STEP 9 until this is resolved.

### STEP 9 — Granola (10 min)
**Paste prompt §A9.**
- Sign up, pay, install desktop app.
- Connect Outlook + Google calendars.
- CfC tells you what API/webhook options exist — you relay that to me later.

Result: account, app installed, integration mode noted.

### STEP 10 — Sentry (3 min)
**Paste prompt §A10.**
Result: DSN in staging note.

### ✅ Hour 1 checkpoint
You should now have, in your staging note:
- GITHUB_REPO_HTTPS / GITHUB_REPO_SSH
- CONVEX_DEPLOY_KEY / CONVEX_URL
- ANTHROPIC_API_KEY (with $100/mo cap, $5/day notification)
- VERCEL_CNAME_TARGET
- GEMINI_API_KEY (with $10/mo budget alert)
- OPENAI_API_KEY (with $30/mo hard limit)
- WISPR Flow installer downloaded (or fallback decided)
- GRANOLA account active
- SENTRY_DSN

If anything is missing, fix before continuing.

---

## HOUR 2 — DNS + OAuth (~50 min)

### STEP 11 — DNS at GoDaddy (5 min)
**Paste prompt §B1.**
- Type: CNAME
- Host: `aryaaos`
- Points to: the VERCEL_CNAME_TARGET from staging note
- TTL: 1 hour

Don't wait for propagation. Move on. Verify at end of Hour 3.

### STEP 12 — Microsoft Graph app registration (12 min) — Layer 1 tenant
**Paste prompt §C1.**

This is the big one. CfC needs you to:
- Be signed into `portal.azure.com` as Layer 1 admin
- Click admin-consent grant button when prompted

Result in staging note:
- MS_CLIENT_ID
- MS_TENANT_ID
- MS_CLIENT_SECRET (the VALUE, not the ID)

### STEP 13 — Google OAuth (8 min)
**Paste prompt §C2.**
Result: GOOGLE_CLIENT_ID + GOOGLE_CLIENT_SECRET.

### STEP 14 — Odoo API key (3 min)
**Paste prompt §C3.**

You confirm or generate:
- ODOO_URL
- ODOO_DB
- ODOO_USER
- ODOO_API_KEY

### STEP 15 — GitHub PAT (5 min)
**Paste prompt §C4.**
- Fine-grained, scoped to `awish4u-jpg/aryaaos` only
- 1-year expiry
Result: GITHUB_PAT in staging note.

### ✅ Hour 2 checkpoint
- DNS record submitted (propagating)
- All OAuth scopes granted
- All keys/secrets in staging note

---

## HOUR 3 — Bulk-load + scaffold (~50 min)

### STEP 16 — Convex env vars (10 min)
**Paste prompt §D1.**

CfC will paste these ~13 env vars from staging into Convex dashboard:
```
ANTHROPIC_API_KEY
GEMINI_API_KEY
OPENAI_API_KEY        (covers Whisper AND embeddings)
ODOO_URL / ODOO_DB / ODOO_USER / ODOO_API_KEY
MS_CLIENT_ID / MS_CLIENT_SECRET / MS_TENANT_ID
GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET
GRANOLA_API_BASE / GRANOLA_API_TOKEN  (if any)
```

**Note**: no `VOYAGE_API_KEY` — we're using OpenAI for embeddings.

### STEP 17 — Vercel env vars (10 min)
**Paste prompt §D2.**

CfC will ask you to open a terminal and run:
```bash
openssl rand -base64 32
```

Paste the output back to CfC as `NEXTAUTH_SECRET`.

CfC pastes these into Vercel:
```
NEXT_PUBLIC_CONVEX_URL
CONVEX_DEPLOY_KEY
NEXTAUTH_URL = https://aryaaos.casaaria.in
NEXTAUTH_SECRET
MS_CLIENT_ID / MS_CLIENT_SECRET / MS_TENANT_ID
GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET
SENTRY_DSN / NEXT_PUBLIC_SENTRY_DSN
```

### STEP 18 — OneDrive folders (3 min)
**Paste prompt §D3.**

Creates `Layer 1/AryaaOS/` with:
- Files
- Backups
- Exports
- Generated Images

### STEP 19 — Claude.ai Project (5 min)
**Paste prompt §D4.**

Creates an AryaaOS project at claude.ai. CfC uploads all 13 bootstrap files as references for ongoing context.

### STEP 20 — Install Wispr Flow (10 min, you do this)
- Run the installer downloaded in STEP 8.
- Sign in with Layer 1 account.
- Set hotkey to **Caps Lock** (single-tap toggles).
- Test by opening Notepad, pressing Caps Lock, speaking — text should appear.

### STEP 21 — Set up Claude Code locally (10 min, you do this)

On your Windows laptop, open PowerShell or terminal:

```powershell
# 1. Install Bun (faster than npm)
powershell -c "irm bun.sh/install.ps1 | iex"

# 2. Verify Node is 20 or higher
node -v

# 3. Install Claude Code
# Get latest install command from code.claude.com/docs

# 4. Clone the empty aryaaos repo
git clone https://github.com/awish4u-jpg/aryaaos.git
cd aryaaos
```

Open Claude Code IN the `aryaaos` folder.

### STEP 22 — Hand off to Claude Code (~30 min, you can step away)

**Paste this exact message into Claude Code:**

```
Read the bootstrap pack at:
https://github.com/awish4u-jpg/Abc/tree/claude/l1os-workspace-build-aAFPi/aryaaos-bootstrap

Important locked decisions:
- Embeddings: OpenAI text-embedding-3-small (NOT Voyage). Schema dims = 1536.
- Cost optimizations from 12-cost-optimization.md apply Day 1:
  - Prompt caching on all stable system prompts
  - Haiku-vs-Sonnet routing per the rules in that doc
  - Content-hash dedup before re-embed
  - Heuristics-before-LLM for trivial inputs
- Hard caps in code: Anthropic $5/day, OpenAI $1/day combined, Gemini $0.50/day
- Sidebar uses full names (Inbox / Pipeline / Clients / etc.), not abbreviations
- Three modes: Stage / Default / Solo
- Pulse top zone reserved on dashboard

Now execute Day 2 of 05-execution-plan.md:

1. Initialize Next.js 15 + TypeScript + Tailwind + App Router
2. Use deps from aryaaos-bootstrap/starter-files/package.json
3. Drop in aryaaos-bootstrap/starter-files/tailwind.config.ts and globals.css
4. Initialize Convex
5. Paste aryaaos-bootstrap/02-convex-schema.ts to convex/schema.ts, deploy
6. Wire Microsoft SSO via NextAuth (Layer 1 tenant) + Convex Auth adapter
7. Wire Sentry from first deploy (both client + server)
8. Build sidebar skeleton per 09-wireframes-v2.md (full names)
9. Build protected /app route showing "Welcome, Awish"
10. Wire nightly Convex export → OneDrive AryaaOS/Backups/yyyy-mm-dd.json
11. Push to main; Vercel auto-deploys
12. Confirm aryaaos.casaaria.in resolves and Microsoft SSO works
13. Tell me green/yellow/red status for each of: HTTPS, Convex deploy,
    SSO redirect, dashboard render, Sentry capture, backup cron

Approve every shell command I run. Use environment variables already
set in Vercel and Convex.
```

Claude Code runs ~30-50 min. Approve commands. **You can step away.** I will watch the chat for any architecture questions it can't answer.

### STEP 23 — Smoke test (5 min)
After Claude Code reports completion:

**Paste prompt §E1 in Claude for Chrome.**

Verifies in order:
1. `https://aryaaos.casaaria.in` loads with valid cert
2. Convex tables visible in dashboard
3. Microsoft SSO redirects + lands on `/app`
4. Omnibox stub triggers (Ctrl+K)
5. Sentry catches a deliberately-thrown error

CfC reports green / yellow / red for each.

If anything is red: **paste the error here in chat**. I'll diagnose.

---

## ✅ DAY 1 DONE WHEN

You log in at `aryaaos.casaaria.in` via Microsoft SSO and see "Welcome, Awish."

You can take Wispr Flow for a spin in any text field. Granola is connected to your calendars.

Total elapsed: ~3 hours of your active time.

---

## DAYS 2-28 — what comes next

Follow `05-execution-plan.md` day by day. Each day:

- ~2h of your active time
- Most code work is Claude Code on your laptop
- I'm in this chat for design/architecture questions and to ship artifacts on specific days

Specific days you'll need me:
- **Day 6**: classifier system prompt
- **Day 10**: Layer 1 design tokens (palette + typography)
- **Day 12**: Odoo data model mapping (your fields → schema fields)
- **Day 18-19**: 6 agent system prompts + CoachAgent
- **Day 21**: nudge prompt copy
- **Day 22**: 300-quote Pulse seed library (JSON)

I'll deliver each on the day, not all upfront.

---

## When to ping me before then

| Trigger | Action |
|---|---|
| Wispr Flow not on Windows in STEP 8 | Tell me — switch to Aqua Voice |
| Anthropic SMS verification fails | Tell me — alternate path exists |
| Convex sign-up rejects GitHub OAuth | Tell me |
| Vercel domain stuck "Invalid Configuration" >45 min | Tell me — DNS issue |
| Microsoft Graph admin consent denied | Tell me — needs IT escalation |
| Claude Code asks an architecture question it can't answer | Tell me — I'll respond inline |
| Anything in STEP 23 smoke test is red | Tell me with the error message |

Otherwise just go.

---

## The whole stack, post-optimization

| Service | Purpose | Monthly cost |
|---|---|---|
| GitHub | Code | $0 |
| Convex | DB + auth + vector + storage + cron | $0-25 |
| Vercel | Hosting | $0 |
| Anthropic | Claude (cached + Haiku-routed + batch) | $20-50 |
| OpenAI | Whisper + embeddings | $2-5 |
| Gemini | Nano Banana images | $1-3 |
| Microsoft 365 | SSO + Mail + Calendar + OneDrive | already paid |
| Odoo | CRM | already paid |
| GoDaddy | DNS | already paid |
| Sentry | Errors | $0 |
| Wispr Flow | Voice dictation | $12-15 |
| Granola | Meeting AI | $14-18 |
| **Total** | | **~$70-90/mo realistic** |

---

## Now go.

Tick STEP 0. Then paste prompt §A1.
